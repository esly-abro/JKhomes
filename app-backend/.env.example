# Server Configuration
NODE_ENV=development
PORT=4000

# ===========================================
# MongoDB Configuration
# ===========================================
# Option 1: MongoDB Atlas (Cloud - Recommended)
# MONGODB_URI=mongodb+srv://leadflow_admin:YOUR_PASSWORD@cluster0.xxxxx.mongodb.net/leadflow?retryWrites=true&w=majority

# Option 2: Local MongoDB
# MONGODB_URI=mongodb://localhost:27017/leadflow

# Leave commented out to use in-memory storage (for testing only)
# MONGODB_URI=

# ===========================================
# Security Configuration
# ===========================================
# JWT Configuration
JWT_SECRET=my-super-secret-jwt-key-for-development-minimum-32-characters-long
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Encryption key for storing sensitive data (exactly 32 characters for AES-256)
# IMPORTANT: Change this in production! Generate with: openssl rand -hex 16
ENCRYPTION_KEY=leadflow-default-key-change-me!!

# ===========================================
# Zoho CRM Configuration (Legacy - ENV based)
# For multi-tenant SaaS, credentials are stored per-organization in database
# ===========================================
ZOHO_CLIENT_ID=your_zoho_client_id
ZOHO_CLIENT_SECRET=your_zoho_client_secret
ZOHO_REFRESH_TOKEN=your_zoho_refresh_token
ZOHO_API_DOMAIN=https://www.zohoapis.in
ZOHO_ACCOUNTS_URL=https://accounts.zoho.in

# Twilio Configuration (for browser-based calling)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890
TWILIO_TWIML_APP_SID=your_twiml_app_sid
TWILIO_API_KEY_SID=your_api_key_sid
TWILIO_API_KEY_SECRET=your_api_key_secret


# Ingestion Service Configuration
INGESTION_SERVICE_URL=http://localhost:3000
INGESTION_SERVICE_API_KEY=dev_api_key_12345

# CORS Configuration
FRONTEND_URL=http://localhost:5173

# ===========================================
# Redis Configuration (BullMQ Workflow Engine)
# ===========================================
# Option 1: Redis URL (takes priority)
# REDIS_URL=redis://:password@localhost:6379/0

# Option 2: Individual settings
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password
REDIS_DB=0

# Worker concurrency (optional tuning)
# WORKFLOW_TRIGGER_CONCURRENCY=5
# WORKFLOW_EXECUTE_CONCURRENCY=10
# WORKFLOW_TIMEOUT_CONCURRENCY=3

# Email Configuration (for user approval notifications)
# Gmail example:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
# For Gmail: Create an "App Password" at https://myaccount.google.com/apppasswords
# DO NOT use your regular Gmail password

# SendGrid example (alternative):
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key